$(() => {
  const $menuListing = $(`
  <section class="menu-listings" id="menu-listings">
      <p>Loading...</p>
    </section>
  `);

  window.$propertyListings =  $menuListing;
  // function listAll(property) {
  //   return generateMenuHTML(property);

  // }

  // window.menuListing= listAll();
});
